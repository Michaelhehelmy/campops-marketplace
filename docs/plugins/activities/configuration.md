# Activities Configuration

> ⚠️ **Plugin-Specific Configuration**

## Enabling
1. Go to **Admin → Plugins → Activities**
2. Click **Install** then **Enable**

This plugin has no runtime configuration options. Activities are managed entirely via the API endpoints. The `activities` table has columns: `type`, `title`, `date`, `price`, and `capacity`.
