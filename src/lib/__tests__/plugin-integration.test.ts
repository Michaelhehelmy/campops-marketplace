/**
 * Plugin inter-communication integration tests have been moved to:
 *   plugins/booking/__tests__/plugin-integration.test.ts
 *
 * These are domain-specific tests (Booking + CRM interaction) and do not
 * belong in the core framework test suite.
 *
 * @see plugins/booking/__tests__/plugin-integration.test.ts
 */

import { describe, it } from 'vitest';

describe('Plugin Inter-communication Integration (moved)', () => {
  it.skip('moved to plugins/booking/__tests__/plugin-integration.test.ts', () => {});
});
