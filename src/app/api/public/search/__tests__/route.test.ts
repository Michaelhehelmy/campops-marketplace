/**
 * Public search route tests moved to resource plugin.
 * Handlers are now registered via plugins/resource/src/routes/index.ts.
 */
import { describe, it } from 'vitest';

describe('Public Search API Route (moved to resource plugin)', () => {
  it.skip('handlers moved to plugins/resource/src/routes/index.ts', () => {});
});
