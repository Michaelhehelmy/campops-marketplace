/**
 * Room management route tests have been moved to:
 *   plugins/booking/__tests__/routes/rooms.test.ts
 *
 * This route is now handled by the booking plugin via the catch-all.
 */
import { describe, it } from 'vitest';

describe('Rooms route tests (moved to plugin)', () => {
  it.skip('moved to plugins/booking/__tests__/routes/rooms.test.ts', () => {});
});
