/**
 * Domain-specific bookings route tests have been moved to:
 *   plugins/booking/__tests__/routes/bookings.test.ts
 *
 * This route is @deprecated — new domain logic belongs in the booking plugin.
 */
import { describe, it } from 'vitest';

describe('Bookings route tests (moved to plugin)', () => {
  it.skip('moved to plugins/booking/__tests__/routes/bookings.test.ts', () => {});
});
