/**
 * Owner/me route tests moved to owner plugin.
 * Handlers are now registered via plugins/owner/src/index.ts.
 */
import { describe, it } from 'vitest';

describe('Owner/me route tests (moved to owner plugin)', () => {
  it.skip('handlers moved to plugins/owner/src/index.ts', () => {});
});
