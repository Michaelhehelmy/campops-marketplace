/**
 * Guest reservations route tests have been moved to the booking plugin.
 * Handlers are now registered via plugins/booking/src/api/routes.ts.
 */
import { describe, it } from 'vitest';

describe('Guest Reservations API Route (moved to plugin)', () => {
  it.skip('handlers moved to plugins/booking/src/api/routes.ts', () => {});
});
