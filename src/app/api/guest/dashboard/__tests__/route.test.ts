/**
 * Domain-specific guest dashboard route tests have been moved to:
 *   plugins/booking/__tests__/routes/guest-dashboard.test.ts
 *
 * This route is @deprecated — guest reservation data belongs in the booking plugin.
 */
import { describe, it } from 'vitest';

describe('Guest dashboard route tests (moved to plugin)', () => {
  it.skip('moved to plugins/booking/__tests__/routes/guest-dashboard.test.ts', () => {});
});
