import type { PluginAPI } from '@sinaicamps/plugin-sdk';

export default async function init(_api: PluginAPI): Promise<void> {
  // HR-core plugin — placeholder for future HR/staff management features.
}
