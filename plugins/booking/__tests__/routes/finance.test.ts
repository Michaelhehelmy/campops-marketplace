/**
 * Finance route tests moved to financial-ops plugin.
 * Handlers are now registered via plugins/financial-ops/src/index.ts.
 */
import { describe, it } from 'vitest';

describe('Finance route tests (moved to financial-ops plugin)', () => {
  it.skip('handlers moved to plugins/financial-ops/src/index.ts', () => {});
});
